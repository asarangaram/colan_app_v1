name: colan_app
description: "Colan App - A Media Repository App"
version: 1.0.0
publish_to: "none"

environment:
  sdk: ^3.6.0

dependencies:
  cl_dart_extensions:
    path: ../cl_dart_extensions
  cl_store:
    path: ../cl_store
  cl_store_local:
    path: ../cl_store_local
  cl_store_server:
    path: ../cl_store_server

  flutter:
    sdk: flutter

dev_dependencies:
  lints: ^3.0.0
  test: ^1.24.0
  very_good_analysis: ^7.0.0

flutter:
  uses-material-design: true
